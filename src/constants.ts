/** Limite de caracteres por resposta de ferramenta, para não estourar o contexto. */
export const CHARACTER_LIMIT = 25000;

/** Quantidade padrão de itens em listagens. */
export const DEFAULT_LIMIT = 25;

/** Teto de itens por página. */
export const MAX_LIMIT = 100;

/** Movimentos incluídos por padrão ao consultar um processo. */
export const DEFAULT_MOVIMENTOS = 30;

export const SERVER_NAME = 'pdpj-mcp-server';
export const SERVER_VERSION = '1.0.0';
